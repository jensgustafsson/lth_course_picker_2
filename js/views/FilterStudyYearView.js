define([
  'jquery',
  'underscore',
  'backbone',
  'text!../templates/FilterStudyYearTemplate.html'
], function ( $, _, Backbone, TimeTableModel, Template) { 

    FilterStudyYearView = Backbone.View.extend({

    });

    return FilterStudyYearView;

});